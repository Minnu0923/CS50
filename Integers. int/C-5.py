#Round rounds the nearest number to the integer
#Periods for decimal points and Comma for separators
x=float(input("What's x? "))
y=float(input("What's y? "))

z=round(x+y)
print(f"{z:,}") #(f"{n:,}"") 